[project]
name = "audit-api"
version = "8.0.0"
description = "AEO/SEO-auditoinnin web-pää — SEO-testi anglesmarketing.fi:lle"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.31",
    "beautifulsoup4>=4.12",
    "lxml>=5.0",
]

[tool.vercel]
entrypoint = "app:app"
